# pnpm Supply Chain Security Settings

# Wait 24 hours before allowing newly published package versions
# This gives security firms time to detect and report malware
minimumReleaseAge=1440

# Prevent installing packages where trust level has decreased
trustPolicy=no-downgrade

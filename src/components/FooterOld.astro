---
import Logo from '@components/Logo.astro';
import Icon from '@components/Icon.astro';

const currentYear = new Date().getFullYear();
---

<footer class="footer">
  {/* Unicorn Studio Canvas - Animated gradient background */}
  <div
    class="unicorn-canvas"
    data-us-project-src="/scenes/gradient-swirl.json"
    data-us-scale="1"
    data-us-dpi="1"
    data-us-lazyload="true"
    data-us-production="true"
    data-us-alttext="Animated footer background with flowing gradients"
  >
  </div>

  {/* Footer Content */}
  <div class="footer-content">
    {/* About & CTA */}
    <div class="footer-main">
      {/* Social Icons */}
      <div class="social-icons">
        <a
          href="https://instagram.com/dues.eckert"
          class="social-icon"
          aria-label="DÜS Eckert auf Instagram"
          target="_blank"
          rel="noopener noreferrer"
        >
          <Icon name="instagram_logo" class="" />
        </a>
        <a
          href="https://www.facebook.com/dues.eckert"
          class="social-icon"
          aria-label="DÜS Eckert auf Facebook"
          target="_blank"
          rel="noopener noreferrer"
        >
          <Icon name="facebook_logo" class="" />
        </a>
        <a
          href="https://www.linkedin.com/company/dueseckert/"
          class="social-icon"
          aria-label="DÜS Eckert auf LinkedIn"
          target="_blank"
          rel="noopener noreferrer"
        >
          <Icon name="linkedin_logo" class="" />
        </a>
      </div>
      <p class="about-text">
        Seit 2004 haben bei DÜS Eckert in Düsseldorf und Essen über 30.000 Menschen Deutsch gelernt.
        Unser gesamtes Team hat ein einziges Ziel: Dass Menschen ihre DTZ-Prüfung bestehen und hier
        ein Leben aufbauen können. <br /> Wir machen nur eins: Integrationskurse, die funktionieren.
      </p>
      <a href="https://dues-eckert.com" class="cta-button" target="_blank" rel="noopener">
        Hauptwebsite besuchen
      </a>
    </div>

    {/* Hero Logo - Huge typography treatment */}
    <div class="hero-logo">
      <Logo textOnly class="logo-text" />
    </div>

    {/* Bottom Bar: Copyright | Legal Links */}
    <div class="footer-bottom">
      <p class="brand-copyright">
        © {currentYear} DÜS Eckert Spracheninstitut GmbH
      </p>

      <nav class="legal-links">
        <a href="https://dues-eckert.com/de/impressum" class="legal-link"> Impressum </a>
        <span class="separator">•</span>
        <a href="https://dues-eckert.com/de/datenschutz" class="legal-link"> Datenschutz </a>
      </nav>
    </div>
  </div>
</footer>

<style>
  .footer {
    position: relative;
    width: 100%;
    min-height: 40rem;
    background-color: #1a1a1a;
    overflow: hidden;
    margin-top: 6rem;
    display: flex;
    flex-direction: column;
  }

  .unicorn-canvas {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 200%;
    height: 200%;
    transform: translate(-50%, -50%);
    z-index: 0;
  }

  .unicorn-canvas::after {
    content: '';
    position: absolute;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.3);
    pointer-events: none;
  }

  .unicorn-canvas > canvas {
    pointer-events: none;
  }

  .footer-content {
    position: relative;
    z-index: 1;
    max-width: var(--container-max-width);
    margin: 0 auto;
    padding: 4rem 2rem 2rem;
    width: 100%;
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 3rem;
  }

  /* Main content area */
  .footer-main {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2rem;
    text-align: center;
    max-width: 42rem;
    margin: 0 auto;
  }

  .about-text {
    font-size: var(--text-body-large);
    line-height: var(--leading-relaxed);
    color: var(--text-secondary);
    margin: 0;
  }

  /* CTA Button - matching HashtagGroup style */
  .cta-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 1.25rem;
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text-primary);
    background: linear-gradient(
      135deg,
      color-mix(in oklch, var(--color-white) 8%, transparent),
      color-mix(in oklch, var(--color-white) 4%, transparent)
    );
    border: 1px solid var(--border-bright);
    border-radius: var(--radius-sm);
    text-decoration: none;
    cursor: pointer;
    transition: all var(--transition-fast);
    backdrop-filter: blur(12px);
  }

  .cta-button:hover {
    transform: translateY(-2px);
    border-color: color-mix(in oklch, var(--color-white) 30%, transparent);
    background: linear-gradient(
      135deg,
      color-mix(in oklch, var(--color-white) 12%, transparent),
      color-mix(in oklch, var(--color-white) 6%, transparent)
    );
  }

  .cta-button:active {
    transform: translateY(0);
  }

  /* Social Icons */
  .social-icons {
    display: flex;
    gap: 1rem;
    align-items: center;
    justify-content: center;
  }

  .social-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    color: var(--color-white);
    text-decoration: none;
    transition: all var(--transition-fast);
    border-radius: 10px;
  }

  .social-icon:hover {
    background-color: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
  }

  .social-icon svg {
    width: 22px;
    height: 22px;
  }

  /* Hero Logo - Full width at bottom */
  .hero-logo {
    width: 100%;
    display: flex;
    justify-content: center;
    color: rgba(255, 255, 255, 0.2);
    align-items: center;
    padding: 2rem 0 0;
  }

  /* Bottom Bar */
  .footer-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .brand-copyright {
    font-size: var(--text-body-small);
    color: rgba(255, 255, 255, 0.5);
    line-height: var(--leading-normal);
    margin: 0;
  }

  .legal-links {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    justify-content: flex-end;
    flex-wrap: wrap;
  }

  .legal-link {
    font-size: var(--text-body-small);
    color: rgba(255, 255, 255, 0.6);
    text-decoration: none;
    transition: color var(--transition-fast);
    white-space: nowrap;
  }

  .legal-link:hover {
    color: var(--color-white);
  }

  .separator {
    color: rgba(255, 255, 255, 0.3);
    user-select: none;
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .footer {
      min-height: 36rem;
    }

    .footer-content {
      padding: 3.5rem 2rem 2rem;
    }
  }

  @media (max-width: 640px) {
    .footer {
      min-height: clamp(26rem, 60vh, 32rem);
    }

    .footer-content {
      padding: 2.5rem 1.5rem 1.5rem;
      gap: 2rem;
    }

    .hero-logo {
      padding: 1.25rem 0 0;
    }

    .footer-main {
      gap: 1.5rem;
    }

    .about-text {
      font-size: var(--text-body-regular);
    }

    .cta-button {
      inline-size: 100%;
    }

    .footer-bottom {
      flex-direction: column;
      align-items: flex-start;
      gap: 1.25rem;
      padding-block-start: 1.5rem;
    }

    .legal-links {
      justify-content: flex-start;
    }

    .social-icon {
      inline-size: 36px;
      block-size: 36px;
    }

    .social-icon svg {
      inline-size: 20px;
      block-size: 20px;
    }
  }
</style>

---
/**
 * Contest Card Component
 * Individual card for contest steps
 */
interface Props {
  number: string;
  title: string;
}

const { number, title } = Astro.props;
---

<article class="contest-card">
  <div class="card-header">
    <div class="card-number">{number}</div>
    <h3>{title}</h3>
  </div>
  <div class="card-content">
    <slot />
  </div>
</article>

<style>
  .contest-card {
    display: grid;
    gap: 1.5rem;
    padding: 2rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-card);
    transition: all var(--transition-base);
    container-type: inline-size;
  }

  .contest-card:hover {
    border-color: var(--border-bright);
    box-shadow: var(--shadow-card-hover);
    transform: translateY(-4px);
  }

  .card-header {
    display: flex;
    gap: 0.75rem;
    align-items: center;
  }

  .card-header h3 {
    margin: 0;
    color: var(--text-primary);
    font-size: clamp(1.375rem, 4cqi, 1.75rem);
    font-weight: 700;
    letter-spacing: var(--tracking-tight);
    text-wrap: balance;
  }

  .card-number {
    display: grid;
    flex-shrink: 0;
    place-items: center;
    inline-size: clamp(2.5rem, 8cqi, 3rem);
    block-size: clamp(2.5rem, 8cqi, 3rem);
    color: var(--text-primary);
    font-size: clamp(0.95rem, 3cqi, 1.1rem);
    font-weight: 800;
    background: var(--gradient-vibrant-subtle);
    border: 1px solid color-mix(in oklch, var(--color-orange-600) 30%, transparent);
    border-radius: 50%;
    box-shadow: var(--shadow-glow-purple);
  }

  .card-content {
    inline-size: 100%;
  }

  /* Container query: Adjust spacing for narrow cards */
  @container (width <= 500px) {
    .contest-card {
      gap: 1.25rem;
      padding: 1.5rem;
    }
  }

  /* Viewport query: Mobile devices */
  @media (width <= 640px) {
    .contest-card {
      gap: 1.125rem;
      padding: 1.5rem;
    }
  }
</style>
